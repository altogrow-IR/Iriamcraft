# Iriamcraft v2 検証記録

実施日: 2026-09-20。WindowsのEdge（Playwrightによる隔離ブラウザ）、390×844と1440×900。ユーザーの実際の保存データにはテストデータを投入していません。

## 自動検証

- `npm test`: 26件成功。既存の建築・床交換・境界・設計図・ライブ反応を維持。
- v1→v2、全形状の配置とRaycast、ドア2セル占有、回転制限・4回転、差分Undo/Redo、範囲削除、水流・障害物・水源削除を確認。
- 10,000 / 20,000 / 50,000件の配置・JSON復元、更新チャンク限定、隣接チャンクの柵更新、色違いの描画共有を確認。
- `npm run lint`: 成功。
- `npm run build`: TypeScript検査・本番ビルド成功。既存Three.jsチャンクの500KB超警告は残る（エラーではない）。

## ブラウザ検証

`tests/browser-check.mjs` を開発サーバーと本番distの `/Iriamcraft/` 配下で実行。

- 実localStorageのv1を読み込み、IndexedDBへ移行、旧データ保持、再読み込みを確認。
- UIでJSON読み込み／ダウンロード、ドア開閉保存、マウス／タッチスワイプ90°回転、4回転、2本指への切り替えとカメラ復帰、Undo/Redo、範囲プレビューと確定削除を確認。
- 板ガラス／ガラスブロック各7色の素材一覧を確認。
- 390pxとPC幅でページの横・縦はみ出しなし。重大なConsole/Pageエラーなし。
- 3連／3×3ブラシ、設計図、矢印・PageDown操作、効果音設定、PNGダウンロード、ライブ開始／終了／結果を別途確認。
- IndexedDBを意図的に拒否した場合も旧保存を表示し、保存失敗を明示。壊れた旧保存の自動上書きがないことを確認。
- 夜のランプが周囲の床を照らすこと、水が落下後に横へ広がることを画像確認。

## 実測（このPC上のブラウザ）

| ワールド | 読み込み＋測定 | 平均フレーム時間 | 参考fps |
| --- | ---: | ---: | ---: |
| 10,000立方体 | 約1.8秒 | 約17ms | 約60 |
| 20,000立方体 | 約2.6秒 | 約17ms | 約60 |
| 50,000立方体 | 約5.3秒 | 約17ms | 約60 |
| 50,000、10形状・13素材混在 | 約3.6秒 | 約17ms | 約60 |

GPU性能はWindows PCの値であり、iPhone/Android実機の値ではありません。立方体テストは100×100の層、混在テストはドア占有に合わせた3マス間隔の層です。視錐台外はカリング対象。長時間の熱・電池消費や全端末でのfpsを保証する測定ではありません。

`outputs/` に画面画像と測定JSONを保存（git管理外）。実機タッチ／GPU、長時間安定性は未確認。

## 公開状態

`base: './'` と既存GitHub Actionsワークフローを維持。本番ビルドを `/Iriamcraft/` 配下に配信し、相対アセット・水Worker・ページ再読み込みを確認しています。GitHub Pagesへの反映状況はリポジトリのActionsで確認します。

## 2026-09-20 マイ設計図・内部ビュー

- npm test: 30件成功。1/100/1001/3482ブロック、全形状、地面除外、底面中央、全64通りのXYZ回転と4方向の合成、衝突、一括Undo/Redoを検証。
- npm run lint / npm run build / TypeScript検査成功。Three.jsの500KB警告は従来同様。
- tests/browser-check.mjs成功: 旧localStorage維持、回転・2本指切替・ドア・範囲削除・JSON・5万ブロックのIndexedDB保存。
- tests/blueprints-browser.mjs成功: 実際のDB version 1を事前作成してversion 2へ更新し、島データの完全一致を確認。範囲から保存、再読み込み、名前変更、配置、Undo/Redo、削除後の再読み込み。3482ブロック保存、blueprintsへのQuotaExceededError注入後もworlds保存成功。
- 390×844、1440×1000で横はみ出しなし。outputs/blueprints-mobile.png、blueprints-desktop.pngで目視確認。
- 実エンジンへのホイール／CDPタッチで通常ズーム→内部、前後移動、カメラ位置を保持した視点回転、2本指移動、ピンチ後退、外観復帰を確認。ガラス壁を通過して水源付きの閉じた部屋内へ進入。near=0.01、ページズーム倍率1を確認。重大なConsole/page errorなし。
- iPhone Safari・Android Chromeの実機、実機GPU性能、Safariのページズーム抑止は未検証。通常ズーム上限10はPC上のモバイル画面で調整。
